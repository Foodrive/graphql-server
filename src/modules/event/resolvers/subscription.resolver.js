const newFoodEvent = () => {
  // TODO pubsub
};

const newRequestEvent = () => {
  // TODO pubsub
};

const foodEventUpdated = () => {
  // TODO pubsub
};

const requestEventUpdated = () => {
  // TODO pubsub
};

const Subscription = {
  newFoodEvent,
  newRequestEvent,
  foodEventUpdated,
  requestEventUpdated,
};

export default {
  Subscription,
};
