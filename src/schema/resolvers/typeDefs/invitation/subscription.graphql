type Subscription {
  createdInvitation(userId: ID!): Invitation
  acceptedInvitation(userId: ID!): Invitation
  rejectedInvitation(userId: ID!): Invitation
  cancelledInvitation(userId: ID!): Invitation
}
