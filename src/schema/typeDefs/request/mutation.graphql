type Mutation {
  createRequest(
    requestorId: ID!
    location: Location!
    numAttendees: Int!
    food: [Food!]!
  ): Request!

  updateRequest(
    requestId: ID!
    location: Location
    numAttendees: Int
    food: [Food!]
  ): Request!

  deleteRequest(requestId: ID!): Boolean!
}
