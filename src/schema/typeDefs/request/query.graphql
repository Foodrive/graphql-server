type Query {
  getRequestsByRequestor(
    requestorId: ID!
    maxRequests: Int
  ): [Request!]!

  getRequestsByLocation(
    coords: Coordinates!
    range: Float!
    maxRequests: Int
  ): [Request!]!
}
