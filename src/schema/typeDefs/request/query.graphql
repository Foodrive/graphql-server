type Query {
  getRequestsByRequestor(
    requestorId: ID!
    maxRequests: Int
  ): [Request!]!

  getRequestsByLocation(
    coords: CoordinatesInput!
    range: Float!
    maxRequests: Int
  ): [Request!]!
}
