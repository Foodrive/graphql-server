type Invitation {
  id: ID!
  event: Event!
  attendeeId: ID!
  attendee: User!
  numAttendees: Int!
  code: String!
  status: InvitationStatus!
  claimedDate: String!
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  REJECTED
  CLAIMED
  CANCELLED
  EXPIRED
}
